{
    "url": "/tools",
    "method": "GET",
    "response": {
        "iterate": "{{body.tools}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.name}}"
        }
    }
}
